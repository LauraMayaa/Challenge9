import { KittenInfosDirective } from './kitten-infos.directive';

describe('KittenInfosDirective', () => {
  it('should create an instance', () => {
    const directive = new KittenInfosDirective();
    expect(directive).toBeTruthy();
  });
});
